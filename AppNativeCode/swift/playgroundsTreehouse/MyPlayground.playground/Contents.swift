//: Playground - noun: a place where people can play

import UIKit  // this code will interface with the uikit framework

var str : String = "Hello"

let modernProgrammingLanguage: String = "Swift"

var greeting = "\(str) \(modernProgrammingLanguage)" // string interpolation || also inferred type

print("To build an iphone app we need to learn \(modernProgrammingLanguage)")






// the height and width of a wall
let height = 120.0 // in inches
let width = 144.0 // in inches

let heightInFeet = height / 12.0
let widthInFeet = width / 12.0

let areaInFeet =  heightInFeet * heightInFeet